make tiles
x these should be buttons
- on first click, moves tiles to player and remaining tiles to center
- on second click, only moves tiles to player
- on hover, shows other tiles which will be taken.
- in original game, requires second click on pattern line.
    - on hover, shows pattern line fill with number of tiles.
make boards
x must have the pattern and floor lines and wall
x wall must have congruent pattern
x score counter can simply be number.
x wall should have translucent tiles to represent that there are not tiles there.
x when colored in, tiles should be disable for clicking.
make center
x flex which automatically starts a new row after 5 tiles enter the center
- leftover tiles should go here
x should start with the '1' tile.
make menu
- options between both games
